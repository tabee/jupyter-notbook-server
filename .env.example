# Jupyter Notebook Server Configuration
# Copy this file to .env and configure your values

# User and Group IDs
# Use your current user/group ID: $(id -u) and $(id -g)
USER_ID=1000
GROUP_ID=1000

# Projects Directory
# Full path to your notebooks directory
PROJECTS_DIR=/home/youruser/jupyter-work

# Jupyter Port
# Local port for Jupyter Notebook (localhost only without Traefik)
JUPYTER_PORT=8888

# Container Name
CONTAINER_NAME=jupyter-notebook

# Domain Configuration (for Traefik)
# Set your domain for external access via Traefik
JUPYTER_DOMAIN=jupyter.example.com

# Traefik Network (optional)
# Use this if you have a custom Traefik network
TRAEFIK_NETWORK=traefik_proxy
